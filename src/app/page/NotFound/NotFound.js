import React from 'react';

const NotFound = () => {
  return (
    <div>
      {/* Renders when no matching url is found */}
      <h1>Sorry, but that page doesn't exist</h1>
    </div>
  );
};

export default NotFound;
