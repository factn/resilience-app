import { OrganizationInterface } from "./schema";
import BaseModel from "./BaseModel";

/**
 * Defines the volunteer.
 *
 * @version 1.0
 */
class Organization extends BaseModel {}

export default new Organization("organizations", {});
